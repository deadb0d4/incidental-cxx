// autogenerated every build
#pragma once

#include <vector>
#include <string_view>
#include <optional>


namespace argparse::parsers {

struct {{ parser.name }} {
  {{ parser.name }}(const std::vector<std::string_view>& args);

  {% for e in parser.options -%}
  {{ e.cpp_typename() }} {{ e.cpp_name() }} { {{ e.cpp_default_value() }} };
  {% endfor -%}

  std::vector<std::string_view> positionals;
};

}
